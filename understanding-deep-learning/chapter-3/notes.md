# chapter 3 - shallow neural networks

## same methodology
an equation represents a family of input/output relations. 

depending on the choice of the parameters we chose a member of this family. 

we fit the data with a training data set by defining a cost function.

we look at how well the model perform on test data.

